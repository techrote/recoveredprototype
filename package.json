{
  "name": "recoveredprototype",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check": "node --check core.js && node --check app.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": { "node": ">=18" }
}
